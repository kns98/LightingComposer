/*
 * The names imported here are intentionally visible throughout the project. This reduces repetitive `using`
 * directives, but it also means adding a namespace here can change unqualified name resolution in every source
 * file, so this should stay limited to genuinely ubiquitous framework and domain namespaces.
 */
global using Xunit;
